---
import { redirect } from '@astrojs/node';

export const prerender = true;

export function getStaticPaths() {
  return [{ params: {}, props: {} }];
}

return new Response(null, {
  status: 301,
  headers: {
    'Location': '/how-fractalism-holds-itself-to-account',
    'Cache-Control': 'public, max-age=31536000',
  },
});
---